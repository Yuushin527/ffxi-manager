PC: 自宅PC

## 2026-05-05 作業内容

### Claude Code セッション管理・確認ルールの導入

- `.claude/settings.json` を新規作成
  - 確認なしで実行可能なコマンドを `allow` に登録（git read/write、ファイル操作、WebFetch、ssh sfreedom@*、scp、wp-cli）
  - 禁止コマンドを `deny` に登録（rm -rf、git push --force、git reset --hard 等）
  - `effortLevel: "high"` を設定

- `CLAUDE.md` を新規作成
  - 確認プロンプトのルール全体をドキュメント化
  - 「始めて」「締めて」セッション管理コマンドの手順を定義
  - TODO コマンドの動作ルールを定義

### 初回セットアップ（始めて）実行

- `.gitignore` に `local-config.md` を追記
- `todo.md` を新規作成
- `work-log/` ディレクトリを作成（`log/` は .gitignore の `[Ll]og/` に引っかかるため改名）
- `local-config.md` を新規作成（PC_NAME: 自宅PC）
