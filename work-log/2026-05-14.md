PC: 職場PC

## 2026-05-14 作業内容

### Codex による Windower→POL 自動遷移問題の調査

Windower 起動後に POL が自動起動しない問題を Codex に診断依頼。

**Codex の診断結果：**

| 優先度 | 内容 |
|---|---|
| 1 | autoPOL のプロキシスレッド起動とhostsエントリ追加のレースコンディション |
| 2 | EU/JP のポート番号が launchAccount（51302）と main（51304）で不一致 |
| 3 | chars bin の POL 自動ログインフラグ（offset 0x6F）が無効 |
| 4 | ClaudeLogin.lua が存在しない |

---

### ClaudeLogin.lua の所在調査

前回実装のログには「Windower/addons/ClaudeLogin/ClaudeLogin.lua 新規作成」とあったが：

- git の当該コミットにファイルが含まれていなかった
- ディスク全体を検索しても見つからなかった
- Windower 4 自体がこの PC に見つからない（インストール場所不明）

→ 実質的に未作成と判断。

---

### ClaudeLogin.lua をゼロから再実装（Codex）

`Windower/addons/ClaudeLogin/ClaudeLogin.lua` を Codex でゼロから新規作成。

**動作フロー：**
1. ロード時に `claudelogin_slot.txt` を読んでスロット番号を取得
2. 8秒待 → Enter（ToS 同意）
3. 6秒待 → Enter（POL メニューで FFXI 選択）
4. 10秒待 → ↓キー × (slot-1) 回 → Enter（キャラ選択）

**修正点：** Codex が出力した Lua バックスラッシュエスケープのバグ（`'\'` → `'\\'`）を修正。

---

### 残課題

- Windower 4 のインストール先を特定して、addon ファイルをコピーする
- Windower の `init.txt` に `lua l ClaudeLogin` が追加されているか確認
- 実機テスト（各ステップの待機時間調整が必要な可能性あり）
