PC: 職場PC

## 2026-05-06 作業内容

### セッション管理ファイルの復旧・改善

- `CLAUDE.md` と `.claude/settings.json` が `.gitignore` 対象になっていた問題を修正
  - 複数PC共有のため `.gitignore` から除外してコミット対象に変更
  - `CLAUDE.md` を再作成（セッション管理コマンド、TODO ルール、技術スタックを定義）
  - `.claude/settings.json` を再作成（git・dotnet コマンドの allow/deny 設定）
- `local-config.md` を作成（PC: 職場PC）

### ビルド試行 → .NET 9 SDK 未インストールを確認

- `dotnet build` を実行したところ .NET 6.0 SDK のみインストールされていることが判明
- このプロジェクトは .NET 9 が必要なためビルド不可
- TODO に「.NET 9 SDK インストール」を追加

### リポジトリ整理・GitHub 同期確認

- セッションが途中で切れていたため状態を確認
- 誤って作成された空ファイル `cd` を削除
- リポジトリは最新状態で GitHub と同期済みであることを確認

---

PC: 自宅PC

## 2026-05-06 作業内容（自宅PC）

### セッション開始・大規模 pull 確認

- 職場PCで行われた変更を pull（171ファイル変更）
  - C# ソース全体が `ffxi-manager/` サブディレクトリに整理
  - `CLAUDE.md` / `.claude/settings.json` がリポジトリ管理に変更
  - `LoginAssistFFXI/`（Python ログインアシスト）、`MultiPOL/`、`PolProxy/` が追加

### autoPOL.exe の動作確認

- 前セッション（Resume で切れたもの）で作成した `autoPOL.exe` を Windower フォルダで確認
- 実行したところ正常動作、ログイン成功を確認

### POL-Proxy をサブモジュールとして追加

- `jaku/POL-Proxy`（Node.js 製、PlayOnline ローディング画面バイパス）を submodule として追加
- 配置：`PolProxy/POL-Proxy/`
- commit & push 済み
